import nascar_api.info
import nascar_api.race
import nascar_api.data

def schedule(year, series):
    """
    Return schedule based on series
    """

""" def standings():
    try:
        standings, wildcard = nhl_api.info.standings()
        return nhl_api.info.Standings(standings, wildcard)
    except:
        return False """